[project]
name = "ai201-project1-unofficial-guide"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = [
    "chromadb>=1.5.9",
    "gradio>=6.9.0",
    "groq>=1.4.0",
    "python-dotenv>=1.2.2",
    "sentence-transformers>=5.5.1",
]



[tool.uv]
python-preference = "only-managed"

[dependency-groups]
dev = [
    "ruff>=0.15.16",
]
